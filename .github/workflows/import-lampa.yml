name: Import Lampa source

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  import:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Import official Lampa source
        run: |
          rm -f lampa-source-main.zip
          rm -rf /tmp/lampa
          git clone --depth 1 https://github.com/yumata/lampa-source.git /tmp/lampa
          cp -a /tmp/lampa/. .
          rm -rf .git .github/workflows/import-lampa.yml
      - name: Commit imported source
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Import official Lampa source' || exit 0
          git push origin HEAD:main
